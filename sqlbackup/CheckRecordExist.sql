SELECT
	START_STATION,
	END_STATION
FROM
	avgtime20170901
WHERE
	DURATION = 0
AND (START_STATION, END_STATION) NOT IN (
	SELECT
		START_STATION,
		END_STATION
	FROM
		avgtime20170908
	WHERE
		DURATION = 0
)
AND (START_STATION, END_STATION) NOT IN (
	SELECT
		START_STATION,
		END_STATION
	FROM
		avgtime20170915
	WHERE
		DURATION = 0
)
AND (START_STATION, END_STATION) NOT IN (
	SELECT
		START_STATION,
		END_STATION
	FROM
		avgtime20170922
	WHERE
		DURATION = 0
)
AND (START_STATION, END_STATION) NOT IN (
	SELECT
		START_STATION,
		END_STATION
	FROM
		avgtime20170929
	WHERE
		DURATION = 0
)